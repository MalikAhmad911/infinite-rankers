I need full technical SEO implementation and server-side crawlability fixes.

You must:

Detect if the site is client-side rendered (SPA React/Vite).

If it is SPA, convert it to fully static-rendered HTML pages OR implement server-side rendering.

Ensure Googlebot can see full HTML content without needing JavaScript execution.

Now implement the following architecture:

1️⃣ Enable Static SEO-Friendly Server

Use Express and ensure pages are served as real HTML.

server.js:

const express = require("express");
const path = require("path");
const fs = require("fs");

const app = express();
const PORT = process.env.PORT || 3000;

app.use(express.static(path.join(__dirname, "public")));

app.get("*", (req, res) => {
  const filePath = path.join(__dirname, "public", req.path, "index.html");
  
  if (fs.existsSync(filePath)) {
    res.sendFile(filePath);
  } else {
    res.status(404).sendFile(path.join(__dirname, "public/404.html"));
  }
});

app.listen(PORT, () => {
  console.log(`Server running on port ${PORT}`);
});
2️⃣ Convert All 91 Pages Into Static HTML

Each page must be structured like:

/public/ai-automation/index.html
/public/seo-services/index.html
/public/digital-marketing/index.html

Each page must include:

<!DOCTYPE html>
<html lang="en">
<head>
  <title>Unique Page Title</title>
  <meta charset="UTF-8" />
  <meta name="description" content="Unique description under 160 characters." />
  <meta name="robots" content="index, follow" />
  <link rel="canonical" href="https://internetrankers.io/page-slug/" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
</head>
<body>
  <h1>Main Keyword Heading</h1>
  <p>Minimum 600 words unique SEO content.</p>
  <a href="/seo-services/">SEO Services</a>
  <a href="/ai-automation/">AI Automation</a>
  <a href="/contact/">Contact</a>
</body>
</html>

All 91 pages must:

Have unique title

Unique meta description

H1

Internal links

Canonical URL

No duplicate content

3️⃣ Auto Generate sitemap.xml

Create script:

/seo/generate-sitemap.js

const fs = require("fs");

const baseUrl = "https://internetrankers.io";

const pages = fs.readdirSync("./public").filter(p => p !== "assets");

const sitemap = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
${pages.map(page => `
<url>
  <loc>${baseUrl}/${page}/</loc>
  <changefreq>weekly</changefreq>
  <priority>0.8</priority>
</url>`).join("")}
</urlset>`;

fs.writeFileSync("./public/sitemap.xml", sitemap);
console.log("Sitemap generated.");

Run automatically on deploy.

4️⃣ Create robots.txt

/public/robots.txt

User-agent: *
Allow: /

Sitemap: https://internetrankers.io/sitemap.xml
5️⃣ Add Structured Data (JSON-LD) On Homepage

Add inside head:

<script type="application/ld+json">
{
 "@context": "https://schema.org",
 "@type": "Organization",
 "name": "Internet Rankers",
 "url": "https://internetrankers.io",
 "logo": "https://internetrankers.io/logo.png",
 "sameAs": [
   "https://linkedin.com/company/internetrankers",
   "https://twitter.com/internetrankers"
 ]
}
</script>
6️⃣ Remove Any Noindex Tags

Search entire project and remove:
<meta name="robots" content="noindex">

7️⃣ Improve Crawl Budget

Create internal HTML sitemap page:

/public/sitemap-page/index.html
List all 91 pages in clickable list.

8️⃣ Performance Optimization

Minify HTML

Compress images

Enable gzip compression

Add compression middleware:

const compression = require("compression");
app.use(compression());
9️⃣ After Deployment

Tell me to:

Submit sitemap to Google Search Console

Inspect URLs manually

Request indexing

Check Coverage report

IMPORTANT:
Do not keep SPA-only rendering.
Do not rely on JavaScript-only content.
Ensure Google can view complete HTML source immediately.

Complete all changes automatically in project.