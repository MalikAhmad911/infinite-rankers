Your task is ONLY to prepare and push this existing project to GitHub professionally.

STRICT RULES:
- DO NOT modify, refactor, optimize, or change ANY website code.
- DO NOT change UI, UX, functionality, database logic, chatbot, APIs, or environment behavior.
- DO NOT update dependencies unless required for Git initialization only.
- The website must remain EXACTLY as it is currently working.

WHAT YOU NEED TO DO:

1. Initialize Git properly if not already initialized.
2. Create a professional .gitignore file suitable for a full stack Node.js + React + PostgreSQL project.

The .gitignore MUST exclude:
- node_modules
- .env files
- build folders
- dist folders
- logs
- cache folders
- temporary files
- database dumps
- Replit system files if unnecessary

3. Verify the project folder structure remains clean and production safe:
   - frontend
   - backend/server
   - database or migrations
   - public assets
   - configuration files
   - package.json files
   - README file

4. Generate a professional README.md containing:
   - Project name
   - Tech stack overview
   - Deployment notes
   - Environment variables placeholder (NO real keys)
   - Basic setup instructions

5. Confirm that sensitive data such as:
   - OpenAI keys
   - SMTP credentials
   - Database credentials
   are NOT pushed to GitHub.

6. Connect this project to a GitHub repository.

7. Stage ALL safe files.

8. Commit using a professional commit message:
   "Initial production-ready project upload"

9. Push the repository to GitHub main branch.

10. Verify repository upload completed successfully.

IMPORTANT:
- Preserve full project functionality.
- Maintain production stability.
- Do not alter dependencies or scripts.
- Only perform Git and repository management tasks.

OUTPUT REQUIRED:
- Confirmation Git initialized
- Confirmation .gitignore created
- Confirmation README created
- Confirmation repository connected
- Confirmation successful push
