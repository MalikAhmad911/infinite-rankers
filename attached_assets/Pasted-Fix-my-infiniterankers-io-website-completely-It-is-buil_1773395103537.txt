Fix my infiniterankers.io website completely. It is built with React + Vite + Express deployed on Hostinger Node.js with GitHub auto-deploy.

GITHUB REPO: https://github.com/MalikAhmad911/infinite-rankers

MY TECH STACK:
- React 18 + TypeScript + Vite 7
- Express 5 backend
- Wouter for routing
- Tailwind CSS
- Build script: tsx script/build.ts
- Build command: tsx script/build.ts
- Start command: NODE_ENV=production node dist/index.cjs
- Hosting: Hostinger Business Node.js, Node 22.x
- GitHub auto-deploy from main branch

CRITICAL ISSUES TO FIX:

=== FIX 1: ADD SEO INJECTION SYSTEM ===

STEP 1 - Create server/seo.ts
Create it with this exact content:

const BASE = "https://infiniterankers.io";
const BRAND = "Infinite Rankers";

interface SEOMeta {
  title: string;
  description: string;
  canonical: string;
}

const STATIC_PAGES: Record<string, SEOMeta> = {
  "/": {
    title: "Infinite Rankers | AI Revenue Growth & Digital Marketing Agency USA",
    description: "We don't just rank you — we grow your revenue. AI automation, SEO, Google Ads, social media marketing for USA businesses. Google Partner. Free strategy call.",
    canonical: `${BASE}/`
  },
  "/about": {
    title: `About ${BRAND} | AI-Powered Digital Marketing Agency`,
    description: "Meet the team behind 500+ business transformations. AI automation experts, Google Partner certified, delivering measurable revenue growth since 2020.",
    canonical: `${BASE}/about`
  },
  "/services": {
    title: `AI Marketing Services | SEO, Google Ads, Automation | ${BRAND}`,
    description: "Complete AI-powered marketing: SEO, Google Ads, social media, AI automation, lead generation. 100+ services. Free consultation available.",
    canonical: `${BASE}/services`
  },
  "/contact": {
    title: `Contact ${BRAND} | Free Strategy Call`,
    description: "Book a free 30-minute strategy call. Get a custom AI marketing plan for your business. We respond within 1 business hour.",
    canonical: `${BASE}/contact`
  },
  "/pricing": {
    title: `Pricing | AI Marketing Plans | ${BRAND}`,
    description: "Transparent pricing. Growth $1,599/month, Scale $2,999/month, Enterprise $5,999/month. AI-powered marketing that pays for itself.",
    canonical: `${BASE}/pricing`
  },
  "/portfolio": {
    title: `Portfolio & Case Studies | Real Results | ${BRAND}`,
    description: "See real results: 340% lead increases, 520% traffic growth, $50M+ revenue generated for clients across USA. View our case studies.",
    canonical: `${BASE}/portfolio`
  },
  "/blog": {
    title: `AI Marketing Blog | SEO & Automation Tips | ${BRAND}`,
    description: "Expert AI marketing insights, SEO strategies, Google Ads tips, and automation guides. Updated weekly with actionable growth strategies.",
    canonical: `${BASE}/blog`
  },
  "/local-business-digital-marketing": {
    title: `#1 Digital Marketing for Local Businesses USA | ${BRAND}`,
    description: "Dominate local search and grow your local business. SEO, Google Maps, social media, and AI automation for local businesses across the USA. Free audit.",
    canonical: `${BASE}/local-business-digital-marketing`
  },
  "/ai-revenue-growth-real-estate": {
    title: `AI Revenue Growth for Real Estate Agents USA | ${BRAND}`,
    description: "AI-powered lead generation and marketing for real estate agents. Automated follow-ups, targeted ads, SEO. Get 50-200 qualified leads per month.",
    canonical: `${BASE}/ai-revenue-growth-real-estate`
  },
  "/ai-revenue-growth-healthcare": {
    title: `AI Marketing for Healthcare & Medical Practices | ${BRAND}`,
    description: "Grow your medical practice with AI marketing. Patient acquisition, reputation management, Google Ads for doctors, dentists, and healthcare providers.",
    canonical: `${BASE}/ai-revenue-growth-healthcare`
  },
  "/infinite-rankers-ai-automation": {
    title: `AI Marketing Automation Services USA | Save 20hrs/Week | ${BRAND}`,
    description: "Complete AI marketing automation. Email, social, ads, CRM on autopilot. n8n, Make.com, Zapier, Voiceflow integrations. Save 20+ hours per week.",
    canonical: `${BASE}/infinite-rankers-ai-automation`
  },
  "/infinite-rankers-paid-advertising": {
    title: `AI-Optimized Paid Advertising | Google & Meta Ads | ${BRAND}`,
    description: "AI-powered Google Ads and Meta Ads delivering 4-6x ROAS. Certified Google Partner. Transparent reporting. Free paid advertising audit.",
    canonical: `${BASE}/infinite-rankers-paid-advertising`
  },
  "/infinite-rankers-seo-services": {
    title: `AI-Powered SEO Services USA | Rank #1 on Google | ${BRAND}`,
    description: "AI-enhanced SEO services that rank you #1 on Google. Technical SEO, content strategy, link building, local SEO. Free SEO audit available.",
    canonical: `${BASE}/infinite-rankers-seo-services`
  },
  "/infinite-rankers-agency": {
    title: `${BRAND} | #1 AI Revenue Growth Agency USA`,
    description: "Infinite Rankers — America's premier AI Revenue Growth Agency. We combine AI automation with proven digital marketing to grow your revenue predictably.",
    canonical: `${BASE}/infinite-rankers-agency`
  },
  "/ai-lead-generation-usa": {
    title: `AI Lead Generation USA | 50-200 Leads/Month | ${BRAND}`,
    description: "AI-powered lead generation delivering 50-200 qualified leads per month. Multi-channel: SEO, PPC, social media, AI calling agents. Free lead gen audit.",
    canonical: `${BASE}/ai-lead-generation-usa`
  },
  "/ai-lead-qualification": {
    title: `AI Lead Qualification Service | Filter & Convert | ${BRAND}`,
    description: "AI agents that qualify leads 24/7. Automated screening, scoring, and routing. Convert more leads with less manual effort. Free demo available.",
    canonical: `${BASE}/ai-lead-qualification`
  },
  "/ai-automation-new-york": {
    title: `AI Marketing Automation New York | NYC Agency | ${BRAND}`,
    description: "AI marketing automation for New York businesses. NYC-focused digital marketing, AI lead generation, Google Ads, SEO. Free NYC strategy consultation.",
    canonical: `${BASE}/ai-automation-new-york`
  },
  "/ai-automation-los-angeles": {
    title: `AI Marketing Automation Los Angeles | LA Agency | ${BRAND}`,
    description: "AI marketing automation for Los Angeles businesses. LA-focused digital marketing, AI lead generation, Google Ads, SEO. Free LA strategy consultation.",
    canonical: `${BASE}/ai-automation-los-angeles`
  },
  "/crm-automation": {
    title: `CRM Automation Services | HubSpot & GoHighLevel | ${BRAND}`,
    description: "Complete CRM automation setup and management. HubSpot, GoHighLevel, Salesforce integrations. Automate your sales pipeline and follow-ups.",
    canonical: `${BASE}/crm-automation`
  },
  "/social-media-marketing": {
    title: `Social Media Marketing Services USA | ${BRAND}`,
    description: "AI-powered social media marketing for Instagram, Facebook, TikTok, LinkedIn. Content creation, scheduling, community management, paid social.",
    canonical: `${BASE}/social-media-marketing`
  },
  "/landing-page-optimization": {
    title: `Landing Page Optimization & CRO Services | ${BRAND}`,
    description: "High-converting landing pages with continuous A/B testing. Increase conversion rates by 50-200%. Free landing page audit available.",
    canonical: `${BASE}/landing-page-optimization`
  },
  "/content-writing": {
    title: `AI Content Writing Services | SEO-Optimized | ${BRAND}`,
    description: "AI-enhanced content writing that ranks on Google and converts visitors. Blog posts, landing pages, case studies. Free content audit available.",
    canonical: `${BASE}/content-writing`
  },
  "/real-estate-lead-generation-ai": {
    title: `Real Estate Lead Generation AI | 50+ Leads/Month | ${BRAND}`,
    description: "AI-powered real estate lead generation. Automated outreach, qualify leads 24/7, integrate with your CRM. Get 50+ qualified buyer and seller leads monthly.",
    canonical: `${BASE}/real-estate-lead-generation-ai`
  },
  "/healthcare-marketing-patient-acquisition": {
    title: `Healthcare Marketing & Patient Acquisition USA | ${BRAND}`,
    description: "Grow your medical practice with targeted digital marketing. Patient acquisition campaigns, reputation management, healthcare SEO. HIPAA-aware marketing.",
    canonical: `${BASE}/healthcare-marketing-patient-acquisition`
  },
  "/seo-vs-paid-ads-strategy": {
    title: `SEO vs Paid Ads: Which Strategy Wins in 2026? | ${BRAND}`,
    description: "Complete comparison of SEO vs Google Ads for USA businesses. When to use each, how to combine both for maximum ROI. Free strategy consultation.",
    canonical: `${BASE}/seo-vs-paid-ads-strategy`
  },
  "/best-seo-companies": {
    title: `Best SEO Companies USA 2026 | Why ${BRAND} Leads`,
    description: "Looking for the best SEO company in the USA? See why Infinite Rankers is rated #1 for AI-powered SEO. Results, pricing, and free audit available.",
    canonical: `${BASE}/best-seo-companies`
  },
  "/services/ai-follow-up": {
    title: `AI Follow-Up Automation | Never Lose a Lead | ${BRAND}`,
    description: "AI-powered follow-up sequences that convert cold leads into paying clients. Automated email, SMS, and voice follow-ups. Free automation demo.",
    canonical: `${BASE}/services/ai-follow-up`
  },
};

const BLOG_POSTS: Record<string, SEOMeta> = {
  "ai-chatbot-lead-conversion": {
    title: `AI Chatbot Lead Conversion Guide 2026 | ${BRAND}`,
    description: "How AI chatbots convert website visitors into leads 4x better than contact forms. Setup guide, best practices, and ROI analysis.",
    canonical: `${BASE}/blog/ai-chatbot-lead-conversion`
  },
  "social-media-marketing-strategy-2025": {
    title: `Social Media Marketing Strategy 2025 | ${BRAND}`,
    description: "Complete social media marketing strategy for 2025. Platform selection, content planning, engagement tactics, and ROI measurement for businesses.",
    canonical: `${BASE}/blog/social-media-marketing-strategy-2025`
  },
};

export function getSEOForRoute(url: string): SEOMeta | null {
  const path = url.split("?")[0].split("#")[0];
  if (STATIC_PAGES[path]) return STATIC_PAGES[path];
  const slug = path.replace(/^\//, "").replace(/\/$/, "");
  if (STATIC_PAGES["/" + slug]) return STATIC_PAGES["/" + slug];
  if (STATIC_PAGES["/" + slug + "/"]) return STATIC_PAGES["/" + slug + "/"];
  const blogMatch = path.match(/^\/blog\/(.+)$/);
  if (blogMatch && BLOG_POSTS[blogMatch[1]]) return BLOG_POSTS[blogMatch[1]];
  return {
    title: `${BRAND} | AI Revenue Growth & Digital Marketing Agency USA`,
    description: "AI-powered digital marketing agency delivering measurable revenue growth. SEO, Google Ads, AI automation for USA businesses. Google Partner certified.",
    canonical: `${BASE}${path}`
  };
}

function getStructuredData(url: string, seo: SEOMeta): string {
  const ORG = {
    "@context": "https://schema.org",
    "@type": ["Organization", "LocalBusiness"],
    "name": "Infinite Rankers",
    "url": BASE,
    "logo": `${BASE}/logo.png`,
    "description": "AI Revenue Growth Agency providing SEO, Google Ads, AI automation and digital marketing for businesses across the USA.",
    "address": {
      "@type": "PostalAddress",
      "streetAddress": "203 N Caroline Pl",
      "addressLocality": "Dover",
      "addressRegion": "DE",
      "postalCode": "19904",
      "addressCountry": "US"
    },
    "contactPoint": {
      "@type": "ContactPoint",
      "telephone": "(703) 415-9373",
      "contactType": "sales",
      "email": "contact@infiniterankers.io",
      "availableLanguage": "English",
      "areaServed": "US"
    },
    "sameAs": [
      "https://infiniterankers.com",
      "https://www.linkedin.com/company/infinite-rankers",
      "https://www.facebook.com/infiniterankers",
      "https://www.crunchbase.com/organization/infinite-rankers"
    ],
    "aggregateRating": {
      "@type": "AggregateRating",
      "ratingValue": "4.9",
      "reviewCount": "87",
      "bestRating": "5"
    }
  };
  const BREADCRUMB = {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": [
      { "@type": "ListItem", "position": 1, "name": "Home", "item": BASE },
      { "@type": "ListItem", "position": 2, "name": seo.title.split("|")[0].trim(), "item": seo.canonical }
    ]
  };
  return JSON.stringify(ORG) + '</script><script type="application/ld+json">' + JSON.stringify(BREADCRUMB);
}

function escapeHtml(s: string): string {
  return s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
}
function escapeAttr(s: string): string {
  return s.replace(/&/g, "&amp;").replace(/"/g, "&quot;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
}

export function injectSEO(html: string, url: string): string {
  const seo = getSEOForRoute(url);
  if (!seo) return html;
  const headEnd = html.indexOf("</head>");
  if (headEnd === -1) return html;
  const tags: string[] = [];
  let result = html;
  const titleTag = `<title>${escapeHtml(seo.title)}</title>`;
  const descTag = `<meta name="description" content="${escapeAttr(seo.description)}" />`;
  const canonicalTag = `<link rel="canonical" href="${escapeAttr(seo.canonical)}" />`;
  if (/<title>[^<]*<\/title>/.test(result)) {
    result = result.replace(/<title>[^<]*<\/title>/, titleTag);
  } else { tags.push(titleTag); }
  if (!/<meta\s+name="description"/.test(result)) tags.push(descTag);
  if (!/<link\s+rel="canonical"/.test(result)) tags.push(canonicalTag);
  tags.push(`<meta property="og:title" content="${escapeAttr(seo.title)}" />`);
  tags.push(`<meta property="og:description" content="${escapeAttr(seo.description)}" />`);
  tags.push(`<meta property="og:url" content="${escapeAttr(seo.canonical)}" />`);
  tags.push(`<meta property="og:type" content="website" />`);
  tags.push(`<meta property="og:site_name" content="Infinite Rankers" />`);
  tags.push(`<meta property="og:image" content="${BASE}/images/og-image.png" />`);
  tags.push(`<meta name="twitter:card" content="summary_large_image" />`);
  tags.push(`<meta name="twitter:title" content="${escapeAttr(seo.title)}" />`);
  tags.push(`<meta name="twitter:description" content="${escapeAttr(seo.description)}" />`);
  tags.push(`<meta name="robots" content="index, follow" />`);
  tags.push(`<script type="application/ld+json">${getStructuredData(url, seo)}</script>`);
  if (tags.length > 0) {
    result = result.replace("</head>", tags.map(t => `  ${t}`).join("\n") + "\n</head>");
  }
  return result;
}

=== FIX 2: UPDATE server/static.ts ===

STEP 2 - Replace the ENTIRE server/static.ts file with:

import { type Express } from "express";
import fs from "fs";
import path from "path";
import express from "express";
import { injectSEO } from "./seo";

export function serveStatic(app: Express) {
  const distPath = path.resolve(__dirname, "public");
  if (!fs.existsSync(distPath)) {
    throw new Error(`Could not find build directory: ${distPath}`);
  }
  app.use(express.static(distPath, { index: false }));
  let render: ((url: string) => string) | null = null;
  const ssrPath = path.resolve(__dirname, "ssr", "entry-server.cjs");
  if (fs.existsSync(ssrPath)) {
    try {
      const ssrModule = require(ssrPath);
      render = ssrModule.render;
      console.log("✅ SSR module loaded");
    } catch (e) {
      console.warn("⚠️ SSR failed, using CSR:", e);
    }
  } else {
    console.warn("⚠️ SSR file not found — using CSR fallback");
  }
  const indexPath = path.resolve(distPath, "index.html");
  const template = fs.readFileSync(indexPath, "utf-8");
  app.use("*splat", (req, res) => {
    let html = template;
    if (render) {
      try {
        const appHtml = render(req.originalUrl);
        html = html.replace("<!--ssr-outlet-->", appHtml);
      } catch (e) {
        console.error("SSR render error:", req.originalUrl, e);
        html = html.replace("<!--ssr-outlet-->", "");
      }
    }
    html = injectSEO(html, req.originalUrl);
    res.status(200).set({
      "Content-Type": "text/html",
      "Cache-Control": "no-cache, no-store, must-revalidate",
    }).end(html);
  });
}

=== FIX 3: ADD SSR ===

STEP 3 - Create client/src/entry-server.tsx:

import React from "react";
import { renderToString } from "react-dom/server";
import { Router } from "wouter";
import App from "./App";

export function render(url: string): string {
  try {
    const staticLocation = () => [url, () => {}] as [string, (to: string) => void];
    return renderToString(
      <Router hook={staticLocation}>
        <App />
      </Router>
    );
  } catch (e) {
    console.error("SSR render error:", e);
    return "";
  }
}

STEP 4 - Update client/index.html
Find: <div id="root"></div>
Replace with: <div id="root"><!--ssr-outlet--></div>
Change nothing else.

STEP 5 - Update client/src/main.tsx
Replace entire file with:

import React from "react";
import { hydrateRoot, createRoot } from "react-dom/client";
import { Router } from "wouter";
import App from "./App";
import "./index.css";

const rootElement = document.getElementById("root")!;

if (rootElement.innerHTML.trim() !== "" && rootElement.innerHTML.trim() !== "<!--ssr-outlet-->") {
  hydrateRoot(rootElement, <Router><App /></Router>);
} else {
  createRoot(rootElement).render(<Router><App /></Router>);
}

=== FIX 4: UPDATE BUILD SCRIPT ===

STEP 6 - Read the existing script/build.ts first.
Then update it to add SSR build step.
The build must produce THREE outputs:
- dist/public/ (client bundle)
- dist/ssr/entry-server.cjs (SSR bundle)
- dist/index.cjs (server bundle)

Add this SSR build step using vite build API:

await viteBuild({
  plugins: [react()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "../client/src"),
      "@shared": path.resolve(__dirname, "../shared"),
      "@assets": path.resolve(__dirname, "../attached_assets"),
    },
  },
  root: path.resolve(__dirname, "../client"),
  build: {
    ssr: true,
    rollupOptions: {
      input: path.resolve(__dirname, "../client/src/entry-server.tsx"),
      output: {
        format: "cjs",
        entryFileNames: "entry-server.cjs",
      },
    },
    outDir: path.resolve(__dirname, "../dist/ssr"),
    emptyOutDir: true,
  },
  ssr: {
    noExternal: ["react", "react-dom", "wouter"],
  },
});

=== FIX 5: BUILD AND TEST ===

STEP 7 - Run build:
Execute: tsx script/build.ts
Wait for completion. Show full output.

Build MUST produce ALL of these:
- dist/public/index.html
- dist/ssr/entry-server.cjs
- dist/index.cjs

If build fails, fix ALL errors before proceeding.

STEP 8 - Test locally:
Run: NODE_ENV=production node dist/index.cjs &
Wait 3 seconds then:
curl -s http://localhost:5000/ | grep -i "infinite rankers" | head -3
curl -s http://localhost:5000/ | grep "title" | head -3
curl -s http://localhost:5000/local-business-digital-marketing | grep "title" | head -3

Output MUST show real HTML — NOT empty root div.

STEP 9 - If SSR fails with window/location error:
Use this simpler entry-server.tsx:

import React from "react";
import { renderToString } from "react-dom/server";
import App from "./App";

export function render(url: string): string {
  try {
    return renderToString(<App />);
  } catch (e) {
    console.error("SSR error:", e);
    return "<div>Loading...</div>";
  }
}

Rebuild and test again.

=== FIX 6: COMMIT AND PUSH ===

STEP 10 - Commit all changes:
git add -A
git commit -m "Fix SSR + SEO injection for infiniterankers.io Hostinger deployment"
git push origin main

Confirm push successful.

=== FINAL CHECKLIST ===
Confirm each:
[ ] tsx script/build.ts completes with zero errors
[ ] dist/public/index.html exists
[ ] dist/ssr/entry-server.cjs exists
[ ] dist/index.cjs exists
[ ] curl shows real HTML with Infinite Rankers content
[ ] curl /local-business-digital-marketing shows correct meta title
[ ] server/seo.ts created with 25+ page SEO data
[ ] GitHub push confirmed

IMPORTANT RULES:
- Do NOT touch React component files in client/src/pages/
- Do NOT touch client/src/components/
- Do NOT touch tailwind.config.ts
- Do NOT change database files
- Tell me STEP X: COMPLETE or STEP X: FAILED after every step
- Show final curl output at the end